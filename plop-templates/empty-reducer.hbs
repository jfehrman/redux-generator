{{#isTypescript}}export interface {{snakeCase name}}State {

};

export interface {{properCase name}}Action {
  type: string,
};

{{/isTypescript}}const DEFAULT{{#isTypescript}}: {{properCase name}}State{{/isTypescript}} = {};

export default (state{{#isTypescript}}: {{properCase name}}State{{/isTypescript}} = DEFAULT, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

